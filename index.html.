<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Strona Mobilna</title>
    <style>
        /* Reset stylów marginesów i paddingów */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Styl strony głównej */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        /* Kontener dla treści strony */
        .container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 100%;
            max-width: 400px; /* Maksymalna szerokość na telefonach */
            text-align: center;
        }

        h1 {
            color: #007BFF;
            font-size: 24px;
            margin-bottom: 15px;
        }

        p {
            font-size: 16px;
            margin-bottom: 20px;
            line-height: 1.5;
        }

        button {
            background-color: #007BFF;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Witamy na naszej stronie mobilnej!</h1>
        <p>Nasza strona jest zoptymalizowana pod kątem urządzeń mobilnych, aby zapewnić wygodne korzystanie na każdym urządzeniu.</p>
        <button>Dowiedz się więcej</button>
    </div>
</body>
</html>
